[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g pnpm@9 && pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @payrail/gateway build",
  "pnpm --filter @payrail/server build"
]

[start]
cmd = "node packages/server/dist/index.js"
