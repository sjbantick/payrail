{
  "name": "@payrail/server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "build": "tsc --project tsconfig.build.json",
    "test": "pnpm run build && node --test dist/*.test.js dist/**/*.test.js",
    "db:migrate": "pnpm run build && node dist/db/migrate.js",
    "db:seed": "pnpm run build && node dist/db/seed.js",
    "settlement:worker": "pnpm run build && node dist/settlement.js",
    "start": "pnpm run build && node dist/index.js"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.11",
    "@payrail/gateway": "workspace:*",
    "hono": "^4.9.7",
    "pg": "^8.20.0",
    "viem": "^2.39.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/pg": "^8.18.0",
    "@typescript-eslint/eslint-plugin": "^8.25.0",
    "@typescript-eslint/parser": "^8.25.0",
    "eslint": "^8.57.1",
    "pg-mem": "^3.0.5",
    "typescript": "^5.8.2"
  }
}
